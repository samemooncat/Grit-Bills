import React from 'react';
import PropTypes from 'prop-types';

const Card = (props) => {
    return(
    	<div>Card</div>
    );
};

Card.propTypes = {
    props: PropTypes.object.isRequired
};

export default Card;