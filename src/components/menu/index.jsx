import React from 'react';
import PropTypes from 'prop-types';

const Menu = (props) => {
    return(
    	<div>Menu</div>
    );
};

Menu.propTypes = {
    props: PropTypes.object.isRequired
};

export default Menu;