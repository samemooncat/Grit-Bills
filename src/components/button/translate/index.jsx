import React from 'react';
import PropTypes from 'prop-types';

const ButtonTranslate = (props) => {
    return(
    	<div>ButtonTranslate</div>
    );
};

ButtonTranslate.propTypes = {
    props: PropTypes.object.isRequired
};

export default ButtonTranslate;